# Python
__pycache__/
*.pyc
.venv/
venv/

# Local data
uploads/
audiobook/
preview_cache/
*.epub

# Local dev files (kept out of the repo)
tts.py
make_test_epub.py

# Local handoff doc (kept out of the repo)
/spec.md

# SSH workaround (broken /etc ssh_config.d symlink)
.sshcfg/
